import { UserTestDirective } from './user-test.directive';

describe('UserTestDirective', () => {
  it('should create an instance', () => {
    const directive = new UserTestDirective();
    expect(directive).toBeTruthy();
  });
});
