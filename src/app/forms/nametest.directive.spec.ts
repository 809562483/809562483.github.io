import { NametestDirective } from './nametest.directive';

describe('NametestDirective', () => {
  it('should create an instance', () => {
    const directive = new NametestDirective();
    expect(directive).toBeTruthy();
  });
});
