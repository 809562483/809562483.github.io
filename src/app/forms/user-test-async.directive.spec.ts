import { UserTestAsyncDirective } from './user-test-async.directive';

describe('UserTestAsyncDirective', () => {
  it('should create an instance', () => {
    const directive = new UserTestAsyncDirective();
    expect(directive).toBeTruthy();
  });
});
